# CLAUDE.md

Read project memory and the active feature doc before editing code.
Run the configured test/lint commands before finishing.
Do not commit or push unless the parent orchestrator explicitly requests it.
